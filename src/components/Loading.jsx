import React from 'react';

const Loading = () => {
  return (
    <div className='flex justify-center mb-30 items-center h-screen'>
      <span className="loading loading-spinner w-10 text-info"></span>
    </div>
  );
};

export default Loading;